# 漫剧智能体 - 服务器部署指南

## 两种使用模式

| 模式 | 说明 | 密钥位置 | 适用场景 |
|------|------|----------|----------|
| **直连模式** | 直接打开 `index.html`，浏览器直连各AI平台 | 浏览器本地（localStorage） | 个人使用，快速体验 |
| **代理模式** | 通过Node.js服务器启动，密钥存在服务器 | 服务器端（环境变量/server.js） | 多人共享、部署上线、密钥安全 |

---

## 快速开始（本地服务器）

### 1. 安装 Node.js
下载地址：https://nodejs.org/ （推荐 LTS 版本，v16+）

### 2. 配置密钥

**方式A：直接在 server.js 中配置（最简单）**
打开 `server.js`，找到 `API_KEYS` 部分（约第20行），填入你的密钥：
```javascript
const API_KEYS = {
  siliconflow: 'sk-你的硅基流动密钥',  // 推荐
  doubao: '',                          // 豆包
  openai: '',                          // OpenAI
  // ...其他平台
};
```

**方式B：使用环境变量（推荐服务器部署）**
复制 `.env.example` 为 `.env`，填入密钥，或在启动时设置环境变量。

### 3. 启动服务器

**Windows用户：**
双击 `启动服务器.bat`

**Mac/Linux用户：**
```bash
chmod +x start.sh
./start.sh
```

**手动启动：**
```bash
cd deploy
npm install
npm start
```

### 4. 访问应用
启动成功后，浏览器访问：**http://localhost:8080**

---

## 部署到公网服务器

### 方案一：VPS/云服务器（腾讯云、阿里云、华为云等）

1. 购买服务器（最低配置 1核2G 即可）
2. 安装 Node.js 环境：
```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# CentOS
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs
```

3. 上传 `deploy` 文件夹到服务器
4. 配置密钥（编辑 server.js 或设置环境变量）
5. 使用 PM2 持久化运行：
```bash
npm install -g pm2
pm2 start server.js --name manga-agent
pm2 save
pm2 startup
```

6. 配置 Nginx 反向代理（可选，支持域名和HTTPS）：
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### 方案二：Docker 部署

创建 `Dockerfile`：
```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install --production
COPY . .
ENV PORT=8080
EXPOSE 8080
CMD ["node", "server.js"]
```

构建并运行：
```bash
docker build -t manga-agent .
docker run -d -p 8080:8080 \
  -e SILICONFLOW_API_KEY=sk-your-key \
  -e DOUBAO_API_KEY=your-doubao-key \
  --name manga-agent manga-agent
```

### 方案三：免费/低成本托管平台

支持以下平台（需自行适配）：
- **Render**：https://render.com （免费计划可用，支持Node.js）
- **Railway**：https://railway.app
- **Vercel**：需将server.js改写为Serverless函数
- **Fly.io**：支持全球部署

---

## 密钥安全说明

### 代理模式的优势
1. **密钥不暴露给浏览器**：用户无法在前端看到API密钥
2. **统一管理**：只需在服务器配置一次，所有用户共享
3. **避免CORS问题**：服务器端调用无跨域限制
4. **支持所有平台**：包括不支持浏览器直连的平台

### 安全建议
1. 生产环境建议配置 HTTPS（Nginx + Let's Encrypt）
2. 可在 server.js 中添加访问密码或Token验证
3. 定期轮换API密钥
4. 设置API调用限额，防止被盗刷

---

## 常用免费API密钥获取

| 平台 | 注册地址 | 免费额度 | 推荐模型 |
|------|----------|----------|----------|
| 硅基流动 | https://cloud.siliconflow.cn/i/rOrOwByo | 注册送14元 | DeepSeek-V4-Pro, Qwen3.7 |
| 豆包 | https://console.volcengine.com/ark | 新用户免费 | Doubao-2.1-Pro |
| DeepSeek | https://platform.deepseek.com/ | 注册送额度 | DeepSeek-V4 |
| 智谱AI | https://open.bigmodel.cn/ | 注册送额度 | GLM-4-Flash |
| 通义千问 | https://dashscope.aliyun.com/ | 免费额度 | Qwen3.7 |

---

## 常见问题

**Q: 启动后访问 http://localhost:8080 还是要我配置密钥？**
A: 检查 server.js 中的 API_KEYS 是否正确填入，启动时控制台会显示哪些平台已配置。

**Q: 如何同时支持直连和代理模式？**
A: 系统自动检测：通过 http://localhost:8080 访问走代理模式；直接双击打开 index.html 走直连模式。

**Q: 可以配置多个密钥吗？**
A: 当前每个平台配置一个密钥。如需多用户支持，可以在server.js中扩展多租户逻辑。

**Q: 手机怎么访问？**
A: 手机和电脑在同一局域网下，手机访问 `http://电脑IP:8080` 即可。
