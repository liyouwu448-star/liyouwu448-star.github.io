# 漫剧智能体 - 完整部署指南

## 架构说明

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  前端 (GitHub    │────▶│  代理服务器        │────▶│  AI平台 API      │
│  Pages/本地)     │     │  (veFaaS/VPS)    │     │  (硅基/豆包等)   │
│                 │     │                  │     │                 │
│  index.html     │     │  server.js       │     │  密钥安全存储    │
│  - 聊天界面      │     │  - /api/chat     │     │  无CORS限制     │
│  - 工作流        │     │  - /api/image    │     │                 │
│  - AI数字员工    │     │  - /api/health   │     │                 │
└─────────────────┘     └──────────────────┘     └─────────────────┘
```

## 两种使用模式

| 模式 | 说明 | 密钥位置 | 适用场景 |
|------|------|----------|----------|
| **直连模式** | 浏览器直连各AI平台 | 浏览器本地(localStorage) | 本地快速体验 |
| **代理模式** | 通过服务器转发请求 | 服务器端(环境变量) | 线上部署、密钥安全 |

---

## 一、前端部署（GitHub Pages）

前端已部署到：https://liyouwu448-star.github.io/

### 更新前端代码
```bash
cd deploy
git add -A
git commit -m "update"
git push origin main
```
GitHub Pages 会自动构建，约1-2分钟后生效。

---

## 二、后端代理部署（三选一）

### 方案A：部署到火山引擎 veFaaS（推荐·Serverless·免费额度）

#### 1. 安装 veFaaS CLI
```bash
npm i -g @volcengine/vefaas-cli@latest
vefaas --version  # 确认 >= 0.2.7
```

#### 2. 登录火山引擎
```bash
vefaas login --sso
```
浏览器会打开火山引擎登录页面，授权后自动返回终端。

#### 3. 初始化项目
在 `deploy` 目录执行：
```bash
vefaas inspect
```
CLI会自动检测项目框架。如果检测不正确，手动指定：
```bash
vefaas config settings --framework nodejs --startCommand "node server.js" --port 8080
```

#### 4. 查看可用网关
```bash
vefaas gateway list --first
```
如果没有网关，需要先在[火山引擎控制台](https://console.volcengine.com/vefaas)创建。

#### 5. 部署应用
```bash
vefaas deploy --newApp --gateway <网关名称>
```

#### 6. 配置API密钥（环境变量）
```bash
# 硅基流动（推荐，免费额度）
vefaas env set --key SILICONFLOW_API_KEY --value sk-你的密钥

# 豆包/火山引擎
vefaas env set --key DOUBAO_API_KEY --value 你的密钥

# 其他平台按需配置
vefaas env set --key DEEPSEEK_API_KEY --value 你的密钥
```

#### 7. 获取访问地址
```bash
vefaas domains
```
会输出类似：`https://xxx.vefaas.xxx.volcengine.com`

#### 8. 在前端配置代理地址
打开 https://liyouwu448-star.github.io/
1. 点击侧边栏「设置」
2. 找到「API代理服务器」
3. 填入上一步获取的 veFaaS 地址
4. 点击「保存代理地址」
5. 点击「测试连接」确认正常

---

### 方案B：部署到VPS/云服务器

#### 1. 安装 Node.js
```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# CentOS
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs
```

#### 2. 上传项目并安装依赖
```bash
cd deploy
npm install
```

#### 3. 配置密钥
```bash
# 方式1：环境变量
export SILICONFLOW_API_KEY=sk-你的密钥
export DOUBAO_API_KEY=你的密钥

# 方式2：编辑 server.js 中的 API_KEYS
# 方式3：创建 .env 文件（复制 .env.example）
```

#### 4. 使用 PM2 持久化运行
```bash
npm install -g pm2
pm2 start server.js --name manga-agent
pm2 save
pm2 startup
```

#### 5. Nginx 反向代理（支持域名+HTTPS）
```nginx
server {
    listen 80;
    server_name your-domain.com;
    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

### 方案C：本地运行（开发测试）

#### Windows
双击 `启动服务器.bat`

#### Mac/Linux
```bash
cd deploy
chmod +x start.sh
./start.sh
```

#### 手动启动
```bash
cd deploy
npm install
SILICONFLOW_API_KEY=sk-你的密钥 npm start
```

访问 http://localhost:8080

---

## 三、API密钥获取

| 平台 | 注册地址 | 免费额度 | 推荐模型 |
|------|----------|----------|----------|
| 硅基流动 | https://cloud.siliconflow.cn/i/rOrOwByo | 注册送14元 | DeepSeek-V4-Pro, Qwen3.6 |
| 豆包 | https://console.volcengine.com/ark | 新用户免费 | Doubao-2.1-Pro |
| DeepSeek | https://platform.deepseek.com/ | 注册送额度 | DeepSeek-V4 |
| 智谱AI | https://open.bigmodel.cn/ | 注册送额度 | GLM-4-Flash |
| 通义千问 | https://dashscope.aliyun.com/ | 免费额度 | Qwen3.6 |

---

## 四、功能模块说明

### 已实现的功能模块

| 模块 | 功能 | 状态 |
|------|------|------|
| 项目工作台 | 项目管理、快速入口 | ✅ |
| 生产管线 | 9阶段全自动生产 | ✅ |
| 新建项目 | 创意→剧本→分镜→成片 | ✅ |
| 小说创作工作流 | 多智能体协作·百万字长篇 | ✅ |
| 2D国漫工作流 | 水墨/二次元/赛璐璐 | ✅ |
| SSS级3D国漫 | 3D建模/动捕/UE5 | ✅ |
| AI真人短剧 | 真人风格短剧 | ✅ |
| 长篇连载 | 自由集数·断点续传 | ✅ |
| 角色资产库 | 跨集一致性 | ✅ |
| Auto派单中心 | 智能路由·自动派发 | ✅ |
| 营销团队 | 营销内容生成 | ✅ |
| **AI数字员工** | 短视频运营·5大子模块 | ✅ |
| AI聊天助手 | 多模型·流式·语音朗读 | ✅ |
| 视频复刻 | 参考视频复刻 | ✅ |
| 灵感库 | 素材收集 | ✅ |
| 引擎设置 | 密钥/代理/引擎配置 | ✅ |

### AI数字员工子模块

| 子模块 | 功能 |
|--------|------|
| 短视频创作 | AI批量生成脚本+配图，支持10+行业 |
| 矩阵分发 | 抖音/小红书/视频号/B站/快手/微博多平台管理 |
| 同城获客 | LBS精准触达+GEO关键词+进度追踪 |
| 智能客服 | 24小时自动回复+线索管理+CSV导出 |
| 数据复盘 | 全域数据看板+AI复盘报告 |
| 语音控制 | 语音指令执行（创作/发布/获客/客服/报告） |

---

## 五、常见问题

### Q: GitHub Pages 上AI功能不工作？
A: GitHub Pages是纯静态托管，浏览器直连AI接口会触发CORS。需要部署后端代理（见方案A或B），然后在前端设置页配置代理地址。

### Q: 如何切换直连/代理模式？
A: 系统自动检测。有代理地址时走代理，没有时走直连。在设置页可配置。

### Q: 手机怎么用？
A: 直接访问 https://liyouwu448-star.github.io/ 即可，已适配移动端。配置好代理后所有功能正常。

### Q: 代理服务器测试连接失败？
A: 检查：
1. 代理服务器是否正常运行
2. 地址是否正确（不要加末尾斜杠）
3. 是否已配置至少一个API密钥
4. 网络是否能访问代理服务器

### Q: 响应速度慢？
A: 可能原因：
1. AI模型推理需要时间（正常10-30秒）
2. 代理服务器冷启动（veFaaS首次请求较慢）
3. 网络延迟
4. 建议使用 DeepSeek-V4-Flash 获得更快的响应
